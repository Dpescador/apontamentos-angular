/**
 * Caminhos reservados para a futura adoção do Angular Router.
 * A aplicação atual possui uma única página e não precisa carregar o pacote
 * @angular/router apenas para renderizar o dashboard principal.
 */
export const APP_ROUTE_PATHS = {
  dashboard: ''
} as const;
