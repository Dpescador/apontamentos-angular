/**
 * Identificadores de navegação usados pelo AppNavigationService.
 * A aplicação continua sem Angular Router porque as páginas são alternadas
 * dentro da mesma aplicação estática publicada no GitHub Pages.
 */
export const APP_ROUTE_PATHS = {
  dashboard: '',
  adminUsers: 'admin/usuarios'
} as const;
